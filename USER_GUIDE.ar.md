# 📖 دليل المستخدم الشامل - ماسح الأمان المتقدم

## 📋 جدول المحتويات

1. [مقدمة](#مقدمة)
2. [التثبيت والإعداد](#التثبيت-والإعداد)
3. [الواجهة الرسومية والويب](#الواجهة-الرسومية-والويب)
4. [المميزات الأساسية](#المميزات-الأساسية)
5. [المميزات المتقدمة](#المميزات-المتقدمة)
6. [التكوين والإعدادات](#التكوين-والإعدادات)
7. [أمثلة عملية](#أمثلة-عملية)
8. [حل المشاكل](#حل-المشاكل)
9. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🌟 مقدمة

### ما هو ماسح الأمان؟

ماسح الأمان هو أداة قوية ومتقدمة للكشف عن:
- 🔑 **الأسرار المخزنة في الكود**: كلمات المرور، مفاتيح API، الرموز المميزة
- 🐛 **الثغرات الأمنية**: أكثر من 50 نوع من الثغرات الأمنية
- 🌐 **فحص المستودعات البعيدة**: فحص GitHub، GitLab، والأرشيفات
- 🎯 **اختبار أمان الويب**: اختبار الصندوق الأسود لتطبيقات الويب
- 📊 **قياس الأداء**: تتبع وتحسين أداء الفحوصات

### لماذا تستخدم ماسح الأمان؟

✅ **دقة عالية**: استخدام الذكاء الاصطناعي لتقليل النتائج الخاطئة بنسبة 90%+
✅ **سهولة الاستخدام**: واجهة تفاعلية جميلة ومعالجات خطوة بخطوة
✅ **إصلاح تلقائي**: إصلاح الثغرات تلقائياً
✅ **تقارير احترافية**: تقارير HTML وJSON وMarkdown
✅ **تكامل CI/CD**: جاهز للدمج في خطوط الأنابيب
✅ **مفتوح المصدر**: مجاني تماماً مع رخصة MIT

---

## 🚀 التثبيت والإعداد

### الخطوة 1: التثبيت

#### الطريقة 1: استخدام pipx (موصى به)

```bash
# تثبيت pipx
pip install pipx

# تثبيت ماسح الأمان
pipx install security-scan-cli
```

**المميزات:**
- ✅ تثبيت معزول ولا يتعارض مع مكتبات أخرى
- ✅ متاح من أي مكان في النظام
- ✅ سهولة التحديث

#### الطريقة 2: من المصدر

```bash
# استنساخ المستودع
git clone https://github.com/ALxxy123/code-scan-sec.git
cd code-scan-sec

# التثبيت
pip install -e .
```

**المميزات:**
- ✅ أحدث الميزات
- ✅ إمكانية التعديل والمساهمة
- ✅ الوصول إلى الكود المصدري

### الخطوة 2: إعداد مفاتيح API

للحصول على أفضل النتائج، قم بإعداد مفتاح API لأحد مزودي الذكاء الاصطناعي:

#### Google Gemini (موصى به - مجاني)

1. اذهب إلى: https://makersuite.google.com/app/apikey
2. سجّل الدخول بحساب Google
3. انقر على "Create API Key"
4. انسخ المفتاح

```bash
# في Linux/Mac
export GEMINI_API_KEY="AIza..."

# في Windows (PowerShell)
$env:GEMINI_API_KEY="AIza..."

# أو أضفه إلى ملف .env
echo 'GEMINI_API_KEY=AIza...' >> .env
```

#### OpenAI (مدفوع)

1. اذهب إلى: https://platform.openai.com/api-keys
2. سجّل الدخول
3. انقر على "Create new secret key"
4. انسخ المفتاح

```bash
export OPENAI_API_KEY="sk-..."
```

#### Anthropic Claude (مدفوع)

1. اذهب إلى: https://console.anthropic.com/
2. سجّل الدخول
3. انقر على "Get API Keys"
4. انسخ المفتاح

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
```

### الخطوة 3: التحقق من التثبيت

```bash
# عرض الإصدار
security-scan version

# عرض المساعدة
security-scan --help

# تشغيل العرض التوضيحي
security-scan demo
```

---

## 🎨 الواجهة الرسومية والويب

### 1. لوحة التحكم المحسّنة (جديد!)

ماسح الأمان يأتي الآن مع لوحة تحكم ويب محسّنة مع دعم كامل للغة العربية!

#### تشغيل الخادم

```bash
# بدء خادم API
python api_server.py

# أو باستخدام uvicorn
uvicorn api_server:app --reload --host 0.0.0.0 --port 8000
```

#### الوصول إلى لوحة التحكم

افتح متصفحك على:
- **لوحة التحكم المحسّنة**: http://localhost:8000/dashboard_enhanced.html
- **لوحة التحكم الأساسية**: http://localhost:8000/dashboard.html
- **توثيق API**: http://localhost:8000/docs

### 2. مميزات لوحة التحكم المحسّنة

#### 🌐 دعم متعدد اللغات
- تبديل فوري بين العربية والإنجليزية
- واجهة كاملة مترجمة
- دعم RTL للعربية

#### 📊 صفحات متعددة
1. **الصفحة الرئيسية**: إحصائيات ورسوم بيانية
2. **المميزات**: عرض جميع المميزات المتاحة
3. **بدء فحص**: واجهة لبدء فحوصات جديدة
4. **الإعدادات**: معالج تكوين تفاعلي
5. **دليل البداية**: تعليمات خطوة بخطوة

#### ⚡ إجراءات سريعة
- بدء فحص جديد بنقرة واحدة
- إعداد مفاتيح API
- الوصول إلى الدليل
- فتح لوحة المراقبة

#### 🎯 أنواع فحص متعددة
- **فحص محلي**: فحص ملفات على جهازك
- **فحص رابط**: فحص مستودعات GitHub/GitLab
- **اختبار صندوق أسود**: اختبار أمان تطبيقات الويب

#### 🔧 معالج الإعدادات
- إعداد مفاتيح API بشكل مرئي
- تكوين إعدادات الفحص
- اختيار تنسيقات التقارير
- حفظ محلي للإعدادات

---

## 🔍 المميزات الأساسية

### 1. كشف الأسرار

#### ما يتم اكتشافه:

- 🔑 كلمات المرور المخزنة
- 🗝️ مفاتيح API (AWS، Google Cloud، Azure، إلخ)
- 🎫 رموز GitHub و GitLab
- 💳 مفاتيح Stripe
- 📧 رموز SendGrid
- 🔐 شهادات SSL الخاصة
- 🗄️ سلاسل اتصال قواعد البيانات

#### مثال استخدام:

```bash
# فحص بسيط
security-scan scan --path .

# فحص مع التحقق بالذكاء الاصطناعي
security-scan scan --path . --ai-provider gemini

# فحص مجلد محدد
security-scan scan --path ./src --output html
```

#### فهم النتائج:

```
🔑 تم اكتشاف سر:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
النوع: AWS Access Key
الملف: config/settings.py:15
القيمة: AKIA****************
الثقة: 95% (تم التحقق بالذكاء الاصطناعي)
الخطورة: 🔴 حرج

التوصية:
1. احذف هذا الملف من التاريخ (git filter-branch)
2. ألغِ المفتاح فوراً
3. استخدم متغيرات البيئة
4. أضف config/ إلى .gitignore
```

### 2. كشف الثغرات

#### أنواع الثغرات المدعومة:

1. **ثغرات الحقن**
   - SQL Injection
   - Command Injection
   - LDAP Injection
   - XPath Injection

2. **Cross-Site Scripting (XSS)**
   - DOM-based XSS
   - Reflected XSS
   - Stored XSS

3. **الفشل التشفيري**
   - خوارزميات التشفير الضعيفة (MD5، SHA1)
   - مفاتيح التشفير المخزنة
   - استخدام Random بدلاً من SecureRandom

4. **سوء التكوين الأمني**
   - وضع التصحيح (Debug) مفعّل
   - CORS مفتوح للجميع
   - عرض رسائل خطأ مفصلة

5. **الوظائف الخطرة**
   - eval()
   - exec()
   - pickle.loads()
   - yaml.load() بدون Loader آمن

#### مثال فحص:

```bash
# فحص الثغرات فقط
security-scan scan --path . --no-ai --output all

# فحص أنواع معينة
security-scan scan --path . --severity critical,high
```

### 3. التقارير

#### تنسيقات التقارير المدعومة:

##### HTML (تقرير تفاعلي)
```bash
security-scan scan --path . --output html
```

**المميزات:**
- 📊 رسوم بيانية تفاعلية
- 🎨 تصميم احترافي
- 🔍 بحث وتصفية
- 📱 متجاوب (يعمل على الموبايل)
- 💾 قابل للمشاركة

##### JSON (للتكامل البرمجي)
```bash
security-scan scan --path . --output json
```

**الاستخدامات:**
- تكامل CI/CD
- معالجة آلية
- تحليل البيانات
- أدوات مخصصة

##### Markdown (للتوثيق)
```bash
security-scan scan --path . --output markdown
```

**الاستخدامات:**
- README في GitHub
- توثيق المشروع
- تقارير للفريق

##### جميع التنسيقات معاً
```bash
security-scan scan --path . --output all
```

---

## 🌟 المميزات المتقدمة

### 1. فحص الروابط البعيدة (v3.2)

#### فحص مستودع GitHub

```bash
# فحص مستودع عام
security-scan scan-url https://github.com/username/repo

# مع الذكاء الاصطناعي
security-scan scan-url https://github.com/username/repo --ai-provider gemini

# مع تقارير كاملة
security-scan scan-url https://github.com/username/repo --output all
```

#### فحص من GitLab

```bash
security-scan scan-url https://gitlab.com/username/project
```

#### فحص ملفات أرشيف

```bash
# ZIP
security-scan scan-url https://example.com/project.zip

# TAR.GZ
security-scan scan-url https://example.com/release.tar.gz

# TAR.BZ2
security-scan scan-url https://releases.example.com/app.tar.bz2
```

#### كيف يعمل؟

1. 📥 تحميل/استنساخ المحتوى
2. 📂 فك الضغط (إذا كان أرشيف)
3. 🔍 فحص كامل
4. 📊 إنشاء تقارير
5. 🗑️ تنظيف تلقائي

### 2. اختبار الصندوق الأسود (v3.2)

#### اختبار شامل لأمان تطبيقات الويب

```bash
# اختبار أساسي
security-scan blackbox https://example.com

# مع مهلة زمنية مخصصة
security-scan blackbox https://example.com --timeout 15

# مع تقرير HTML
security-scan blackbox https://example.com --output html
```

#### الاختبارات المنفذة:

1. **رؤوس الأمان**
   - HSTS (HTTP Strict Transport Security)
   - CSP (Content Security Policy)
   - X-Frame-Options
   - X-Content-Type-Options
   - X-XSS-Protection
   - Referrer-Policy

2. **SSL/TLS**
   - صحة الشهادة
   - تاريخ انتهاء الصلاحية
   - إصدار TLS
   - قوة التشفير

3. **ثغرات الحقن**
   - SQL Injection (20+ حمولة)
   - XSS (Reflected & Stored)
   - Command Injection
   - Path Traversal

4. **أمان ملفات تعريف الارتباط**
   - علامة Secure
   - علامة HttpOnly
   - SameSite

#### مثال نتيجة:

```
🎯 نتائج اختبار الصندوق الأسود
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

الموقع: https://example.com
المدة: 12.5 ثانية

✅ رؤوس الأمان: 7/10
  ✓ HSTS موجود
  ✓ X-Frame-Options موجود
  ✗ CSP مفقود
  ✗ Referrer-Policy مفقود

✅ SSL/TLS: ممتاز
  ✓ شهادة صالحة
  ✓ TLS 1.3
  ✓ تشفير قوي

⚠️ ثغرات محتملة: 2
  🔴 SQL Injection محتمل في /search
  🟡 Reflected XSS محتمل في /profile

التوصيات:
1. أضف Content-Security-Policy
2. راجع /search للحماية من SQL Injection
3. قم بعمل escape للمدخلات في /profile
```

### 3. الإصلاح التلقائي

#### أنواع الإصلاحات المدعومة:

1. **التشفير الضعيف**
   ```python
   # قبل
   import hashlib
   hash = hashlib.md5(password).hexdigest()

   # بعد (تلقائي)
   import hashlib
   hash = hashlib.sha256(password.encode()).hexdigest()
   ```

2. **الأسرار المخزنة**
   ```python
   # قبل
   API_KEY = "sk-1234567890"

   # بعد (تلقائي)
   import os
   API_KEY = os.getenv("API_KEY")
   ```

   + يتم إنشاء ملف `.env.example`:
   ```
   API_KEY=your-api-key-here
   ```

3. **الوظائف الخطرة**
   ```python
   # قبل
   result = eval(user_input)

   # بعد (تلقائي)
   import ast
   result = ast.literal_eval(user_input)
   ```

#### استخدام الإصلاح التلقائي:

```bash
# عرض ما سيتم إصلاحه (بدون تغيير)
security-scan auto-fix --path . --dry-run

# إصلاح تفاعلي (يسألك قبل كل تغيير)
security-scan auto-fix --path .

# إصلاح تلقائي (بدون سؤال)
security-scan auto-fix --path . --no-interactive

# إصلاح أنواع محددة فقط
security-scan auto-fix --path . --fix-types crypto secrets

# إصلاح ملف محدد
security-scan auto-fix --path ./src/config.py
```

#### الأمان:

✅ يتم إنشاء نسخ احتياطية تلقائياً (*.backup)
✅ يمكن الرجوع عن التغييرات
✅ مراجعة قبل التطبيق (في الوضع التفاعلي)

### 4. قياس الأداء (v3.2)

#### تتبع وتحسين أداء الفحوصات

```bash
# قياس أداء أساسي
security-scan benchmark-scan /path/to/project

# إنشاء أساس مسمى
security-scan benchmark-scan /path/to/project --name "baseline-v1.0"

# قياس بدون مقارنة
security-scan benchmark-scan /path/to/project --no-compare
```

#### المقاييس المتتبعة:

- ⏱️ **المدة الزمنية**: إجمالي وقت الفحص
- 📊 **الإنتاجية**:
  - ملفات/ثانية
  - أسطر/ثانية
  - نتائج/ثانية
- 💾 **الذاكرة**:
  - الاستخدام الأولي
  - ذروة الاستخدام
  - المتوسط
- 🔄 **CPU**: نسبة الاستخدام
- 🤖 **الذكاء الاصطناعي**:
  - عدد الاستدعاءات
  - متوسط وقت الاستجابة
  - معدل النجاح

#### مثال نتيجة:

```
📊 نتائج قياس الأداء
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

الاسم: baseline-v1.0
التاريخ: 2025-01-15 14:30:00

⏱️ الأداء:
  المدة: 45.2 ثانية
  الإنتاجية: 15.3 ملف/ث
  الأسطر: 1,234 سطر/ث

💾 الموارد:
  الذاكرة (ذروة): 256 MB
  CPU (متوسط): 45%

🤖 الذكاء الاصطناعي:
  الاستدعاءات: 123
  الاستجابة: 1.2 ث/استدعاء
  النجاح: 98.4%

📈 المقارنة مع baseline-v1.0:
  السرعة: ⬆️ +15% أسرع
  الذاكرة: ⬇️ -8% أقل
  الدقة: ➡️ نفس المستوى
```

---

## ⚙️ التكوين والإعدادات

### ملف config.yaml

أنشئ ملف `config.yaml` في جذر مشروعك:

```yaml
# إعدادات الفحص
scan:
  # عتبة الإنتروبيا (0-10)
  # القيمة الأعلى = نتائج أقل ولكن أكثر دقة
  entropy_threshold: 3.5

  # تفعيل التحقق بالذكاء الاصطناعي
  enable_ai_verification: true

  # تفعيل فحص الثغرات
  enable_vulnerability_scan: true

  # الحد الأقصى لحجم الملف (MB)
  max_file_size: 10

  # عدد الخيوط للمعالجة المتوازية
  max_workers: 4

# إعدادات الذكاء الاصطناعي
ai:
  # المزود الافتراضي (gemini/openai/claude)
  default_provider: gemini

  # الحد الأقصى لمحاولات إعادة المحاولة
  max_retries: 5

  # المهلة الزمنية للطلب (ثانية)
  timeout: 30

  # حجم الدفعة
  batch_size: 10

# إعدادات الثغرات
vulnerabilities:
  # مستويات الخطورة المراد البحث عنها
  severity_levels:
    - critical
    - high
    - medium
    - low

  # فئات الثغرات
  categories:
    - sql_injection
    - xss
    - command_injection
    - weak_crypto
    - dangerous_functions
    - path_traversal
    - ssrf
    - xxe

  # تضمين الثغرات منخفضة الخطورة
  include_low_severity: false

# إعدادات التقارير
report:
  # مجلد الإخراج
  output_dir: output

  # التنسيقات الافتراضية
  default_formats:
    - html
    - json

  # فتح التقرير في المتصفح تلقائياً
  auto_open_browser: true

  # تضمين الكود المصدري في التقارير
  include_source_code: true

  # عدد الأسطر من السياق
  context_lines: 3

# الأنماط المستثناة
exclude:
  # أنماط الملفات
  patterns:
    - "*.min.js"
    - "*.min.css"
    - "node_modules/**"
    - "venv/**"
    - ".git/**"
    - "dist/**"
    - "build/**"

  # امتدادات الملفات
  extensions:
    - .jpg
    - .png
    - .gif
    - .pdf
    - .zip

# إعدادات الإصلاح التلقائي
auto_fix:
  # إنشاء نسخ احتياطية
  create_backups: true

  # لاحقة ملف النسخ الاحتياطي
  backup_suffix: .backup

  # أنواع الإصلاحات المفعلة
  enabled_fixes:
    - weak_crypto
    - hardcoded_secrets
    - dangerous_functions

  # الوضع التفاعلي
  interactive_mode: true
```

### متغيرات البيئة

يمكن استخدام ملف `.env` لتخزين المتغيرات:

```bash
# مفاتيح API
GEMINI_API_KEY=AIza...
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# إعدادات الفحص
ENTROPY_THRESHOLD=3.5
ENABLE_AI=true
ENABLE_VULN_SCAN=true

# إعدادات التقارير
OUTPUT_DIR=./security-reports
AUTO_OPEN_BROWSER=true
```

---

## 💡 أمثلة عملية

### مثال 1: فحص مشروع Django

```bash
# فحص كامل مع جميع التقارير
security-scan scan \
  --path ./my-django-project \
  --ai-provider gemini \
  --output all

# الإصلاح التلقائي
security-scan auto-fix \
  --path ./my-django-project \
  --fix-types secrets crypto
```

### مثال 2: فحص مكتبة Node.js

```bash
# فحص بدون مجلد node_modules
security-scan scan \
  --path . \
  --exclude "node_modules/**,dist/**" \
  --output json
```

### مثال 3: فحص API Flask

```bash
# فحص الثغرات فقط
security-scan scan \
  --path ./flask-api \
  --no-ai \
  --severity critical,high \
  --output html
```

### مثال 4: فحص مستودع مفتوح المصدر

```bash
# فحص من GitHub
security-scan scan-url \
  https://github.com/django/django \
  --ai-provider gemini \
  --output all
```

### مثال 5: اختبار API في الإنتاج

```bash
# اختبار صندوق أسود
security-scan blackbox \
  https://api.myapp.com \
  --timeout 20 \
  --output html
```

### مثال 6: تكامل مع GitHub Actions

```yaml
name: Security Scan

on: [push, pull_request]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Scanner
        run: pip install security-scan-cli

      - name: Run Security Scan
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          security-scan scan \
            --path . \
            --ai-provider gemini \
            --output json

      - name: Upload Report
        uses: actions/upload-artifact@v3
        with:
          name: security-report
          path: output/report.json
```

---

## 🔧 حل المشاكل

### المشكلة 1: خطأ في مفتاح API

**الخطأ:**
```
Error: Invalid API key for Gemini
```

**الحل:**
1. تأكد من صحة المفتاح
2. تحقق من أن المتغير مُعرّف:
   ```bash
   echo $GEMINI_API_KEY
   ```
3. أعد تعريف المتغير:
   ```bash
   export GEMINI_API_KEY="your-key"
   ```

### المشكلة 2: لا يتم اكتشاف ملفات

**الخطأ:**
```
No files found to scan
```

**الحل:**
1. تحقق من المسار:
   ```bash
   ls -la /path/to/scan
   ```
2. تحقق من أنماط الاستثناء في config.yaml
3. تحقق من أذونات الملفات

### المشكلة 3: بطء الفحص

**الأسباب المحتملة:**
- عدد كبير من الملفات
- استخدام الذكاء الاصطناعي على جميع النتائج
- عتبة إنتروبيا منخفضة جداً

**الحلول:**
1. استخدم الاستثناءات:
   ```bash
   --exclude "node_modules/**,venv/**"
   ```

2. ارفع عتبة الإنتروبيا:
   ```bash
   --entropy-threshold 4.0
   ```

3. استخدم المعالجة المتوازية:
   ```yaml
   scan:
     max_workers: 8
   ```

### المشكلة 4: نتائج خاطئة كثيرة

**الحل:**
1. فعّل التحقق بالذكاء الاصطناعي:
   ```bash
   --ai-provider gemini
   ```

2. ارفع عتبة الإنتروبيا:
   ```bash
   --entropy-threshold 4.5
   ```

3. استخدم ملف ignore:
   ```bash
   echo "false-positive-pattern" >> .scanignore
   ```

---

## ❓ الأسئلة الشائعة

### هل الأداة مجانية؟
نعم، ماسح الأمان مفتوح المصدر تماماً برخصة MIT. ومع ذلك، بعض مزودي الذكاء الاصطناعي (OpenAI، Claude) مدفوعة، بينما Gemini مجاني.

### هل أحتاج إلى مفتاح API؟
لا، يمكنك استخدام الأداة بدون ذكاء اصطناعي. لكن استخدام الذكاء الاصطناعي يحسّن الدقة بشكل كبير.

### هل يمكن استخدامها في CI/CD؟
نعم! الأداة مصممة للتكامل السهل مع GitHub Actions و GitLab CI و Jenkins وغيرها.

### كم من الوقت يستغرق الفحص؟
يعتمد على:
- حجم المشروع
- استخدام الذكاء الاصطناعي
- سرعة الإنترنت (للذكاء الاصطناعي)

عادةً:
- مشروع صغير (< 100 ملف): 10-30 ثانية
- مشروع متوسط (100-1000 ملف): 1-5 دقائق
- مشروع كبير (> 1000 ملف): 5-30 دقيقة

### هل يمكن فحص مستودعات خاصة؟
نعم، لكن تحتاج إلى استنساخها محلياً أولاً أو استخدام رموز وصول.

### هل البيانات آمنة؟
نعم:
- الفحص يتم محلياً
- فقط الأنماط المشتبه بها ترسل للذكاء الاصطناعي (وليس كامل الكود)
- لا يتم تخزين أي بيانات في السحابة
- جميع التقارير محلية

### كيف أبلغ عن مشكلة؟
1. تحقق من Issues الموجودة: https://github.com/ALxxy123/code-scan-sec/issues
2. إذا لم تجد مشكلتك، أنشئ issue جديد
3. ضمّن:
   - نسخة الأداة
   - رسالة الخطأ الكاملة
   - خطوات إعادة الإنتاج

### كيف أساهم؟
مرحباً بك! راجع [CONTRIBUTING.md](CONTRIBUTING.md)

---

## 📞 الدعم والمساعدة

### الموارد المفيدة
- 📖 [التوثيق الكامل](README.ar.md)
- 🐛 [الإبلاغ عن مشاكل](https://github.com/ALxxy123/code-scan-sec/issues)
- 💬 [المناقشات](https://github.com/ALxxy123/code-scan-sec/discussions)
- 🌟 [GitHub](https://github.com/ALxxy123/code-scan-sec)

### الحصول على مساعدة
1. ابحث في الأسئلة الشائعة أعلاه
2. تحقق من Issues المغلقة
3. اسأل في Discussions
4. أنشئ Issue جديد إذا لزم الأمر

---

**صُنع بـ ❤️ للمطورين ومهندسي الأمان**
