# دليل استخدام Security Scan CLI 🔒

دليل شامل لاستخدام أداة فحص الأمان الاحترافية - الإصدار 4.0.0

## 📋 جدول المحتويات

1. [مقدمة](#مقدمة)
2. [التثبيت](#التثبيت)
3. [الأوامر المتاحة](#الأوامر-المتاحة)
4. [أمثلة الاستخدام](#أمثلة-الاستخدام)
5. [الأخطاء الشائعة وحلولها](#الأخطاء-الشائعة-وحلولها)
6. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## مقدمة

**Security Scan CLI** هي أداة احترافية لفحص الأمان تساعد المطورين على:
- كشف الأسرار المخزنة في الكود (API Keys, Passwords, Tokens)
- اكتشاف الثغرات الأمنية (SQL Injection, XSS, وغيرها)
- فحص التكوينات غير الآمنة
- اكتشاف أنماط الكود الخطرة

---

## التثبيت

### من PyPI (الطريقة الموصى بها)

```bash
pip install security-scan-cli
```

### من المصدر

```bash
git clone https://github.com/yourusername/code-scan-sec.git
cd code-scan-sec
pip install -e .
```

### التحقق من التثبيت

```bash
security-scan version
```

---

## الأوامر المتاحة

### 1. القائمة التفاعلية (Interactive Menu)

**الأمر:**
```bash
security-scan interactive
```
أو
```bash
security-scan menu
```

**الوصف:** يعرض قائمة تفاعلية لاختيار الأمر المطلوب

**متى تستخدمه:**
- عندما لا تتذكر الأوامر المتاحة
- للمستخدمين الجدد الذين يريدون استكشاف الأداة

---

### 2. فحص مشروع محلي (Scan Local)

**الأمر:**
```bash
security-scan scan-local <المسار>
```

**الخيارات:**
- `--output, -o`: مجلد التقارير (افتراضي: `output`)
- `--format, -f`: صيغة التقرير (`pdf`, `csv`, `json`, `html`, `all`)
- `--no-ai`: تعطيل التحقق بالذكاء الاصطناعي
- `--quiet, -q`: تقليل النواتج المعروضة

**أمثلة:**
```bash
# فحص المشروع الحالي
security-scan scan-local .

# فحص مجلد محدد
security-scan scan-local /path/to/project

# فحص مع تحديد صيغة التقرير
security-scan scan-local ./myproject --format pdf

# فحص بدون AI وبشكل صامت
security-scan scan-local . --no-ai --quiet
```

**ماذا يفحص:**
- الأسرار المخزنة في الكود (API Keys، كلمات المرور)
- الثغرات الأمنية (SQL Injection، XSS)
- التكوينات غير الآمنة
- أنماط الكود الخطرة

---

### 3. فحص رابط URL

**الأمر:**
```bash
security-scan scan-url <URL>
```

**الخيارات:**
- `--output, -o`: مجلد التقارير (افتراضي: `output`)
- `--format, -f`: صيغة التقرير (`pdf`, `csv`, `json`, `html`, `all`)
- `--quiet, -q`: تقليل النواتج المعروضة

**أمثلة:**
```bash
# فحص موقع ويب
security-scan scan-url https://example.com

# فحص مستودع GitHub
security-scan scan-url https://github.com/user/repo

# فحص مع تحديد صيغة التقرير
security-scan scan-url https://example.com --format json
```

**ماذا يفحص:**
- تحليل رؤوس الأمان (Security Headers)
- فحص تكوين الخادم
- التحقق من SSL/TLS
- تحليل robots.txt
- فحص المسارات الشائعة

---

### 4. الفحص الأسود (Black-box Testing)

**الأمر:**
```bash
security-scan scan-blackbox <URL>
```

**الخيارات:**
- `--output, -o`: مجلد التقارير (افتراضي: `output`)
- `--format, -f`: صيغة التقرير
- `--timeout, -t`: مهلة الطلبات بالثواني (افتراضي: 30)
- `--quiet, -q`: تقليل النواتج المعروضة

**أمثلة:**
```bash
# فحص أسود لموقع
security-scan scan-blackbox https://example.com

# فحص مع تحديد مهلة زمنية
security-scan scan-blackbox https://api.example.com --timeout 60
```

**ملاحظة:** يقوم بفحوصات آمنة وسلبية فقط (Safe, Passive Checks)

**ماذا يفحص:**
- رؤوس الأمان
- أمان الكوكيز (Cookies)
- تكوين SSL/TLS
- التكوينات الخاطئة الشائعة
- تسريب المعلومات

---

### 5. قياس الأداء (Benchmark)

**الأمر:**
```bash
security-scan benchmark <المسار أو URL>
```

**الخيارات:**
- `--name, -n`: اسم القياس

**أمثلة:**
```bash
# قياس أداء فحص مشروع
security-scan benchmark ./myproject

# قياس أداء مع تسمية القياس
security-scan benchmark /path/to/code --name "my-benchmark"
```

**ماذا يقيس:**
- مدة الفحص
- عدد الملفات/الأسطر المعالجة في الثانية
- استخدام الذاكرة
- استخدام المعالج

---

### 6. التحقق من التحديثات

**الأمر:**
```bash
security-scan check-update
```

**الوصف:** يتحقق من وجود تحديثات جديدة للأداة

---

### 7. عرض معلومات الإصدار

**الأمر:**
```bash
security-scan version
```

**الوصف:** يعرض معلومات الإصدار الحالي

---

## الأخطاء الشائعة وحلولها

### ❌ خطأ: `No such command 'interactive'`

**السبب:** الإصدار القديم لا يحتوي على الأمر `interactive`

**الحل:**
```bash
# قم بتحديث الأداة أو استخدم الأمر البديل
security-scan menu
```

---

### ❌ خطأ: `The term 'scan-local' is not recognized`

**السبب:** محاولة تشغيل الأمر الفرعي بدون الأمر الرئيسي `security-scan`

**الحل الخاطئ:**
```bash
scan-local .           # ❌ خاطئ
check-update           # ❌ خاطئ
```

**الحل الصحيح:**
```bash
security-scan scan-local .        # ✅ صحيح
security-scan check-update        # ✅ صحيح
```

---

### ❌ خطأ: `No such option: --scan-local`

**السبب:** استخدام الأمر الفرعي كخيار بدلاً من أمر

**الحل الخاطئ:**
```bash
security-scan --scan-local .      # ❌ خاطئ
security-scan --check-update      # ❌ خاطئ
```

**الحل الصحيح:**
```bash
security-scan scan-local .        # ✅ صحيح
security-scan check-update        # ✅ صحيح
```

---

### ❌ خطأ: Missing required argument

**السبب:** عدم تمرير المسار أو URL المطلوب

**الحل الخاطئ:**
```bash
security-scan scan-local          # ❌ خاطئ (يحتاج مسار)
security-scan scan-url            # ❌ خاطئ (يحتاج URL)
```

**الحل الصحيح:**
```bash
security-scan scan-local .                  # ✅ صحيح
security-scan scan-url https://example.com  # ✅ صحيح
```

---

## الأسئلة الشائعة

### س: ما الفرق بين `scan-local` و `scan-url`؟

**ج:**
- `scan-local`: لفحص الملفات والمجلدات المحلية على جهازك
- `scan-url`: لفحص المواقع أو المستودعات البعيدة (Remote)

---

### س: ما الفرق بين `scan-url` و `scan-blackbox`؟

**ج:**
- `scan-url`: يفحص الرابط ويحلل رؤوس الأمان والتكوينات
- `scan-blackbox`: يقوم بفحص أمني شامل (Black-box testing) بدون الوصول للكود المصدري

---

### س: هل يمكنني استخدام الأداة على Windows؟

**ج:** نعم! الأداة تعمل على:
- Windows (PowerShell, CMD)
- macOS
- Linux

---

### س: كيف أحصل على تقرير PDF فقط؟

**ج:**
```bash
security-scan scan-local . --format pdf
```

---

### س: كيف أعطل الفحص بالذكاء الاصطناعي؟

**ج:**
```bash
security-scan scan-local . --no-ai
```

---

### س: أين تُحفظ التقارير؟

**ج:** افتراضياً في مجلد `output`، يمكنك تغييره:
```bash
security-scan scan-local . --output my-reports
```

---

## الصيغة العامة للأوامر

```
security-scan <الأمر> [المعاملات] [الخيارات]
```

**مثال:**
```bash
security-scan scan-local ./myproject --format pdf --quiet
              ↓          ↓           ↓               ↓
           الأمر      المعامل      خيار 1        خيار 2
```

---

## نصائح الاستخدام

### ✅ للمبتدئين

ابدأ بالقائمة التفاعلية:
```bash
security-scan interactive
```

### ✅ للاستخدام السريع

فحص سريع للمشروع الحالي:
```bash
security-scan scan-local . --quiet
```

### ✅ للحصول على تقارير شاملة

```bash
security-scan scan-local . --format all
```

### ✅ للأداء الأفضل

عطّل AI إذا كنت لا تحتاجه:
```bash
security-scan scan-local . --no-ai
```

---

## الدعم والمساعدة

### الحصول على مساعدة

```bash
security-scan --help                    # مساعدة عامة
security-scan scan-local --help         # مساعدة لأمر محدد
```

### الإبلاغ عن مشاكل

قم بالإبلاغ عن المشاكل على GitHub:
```
https://github.com/yourusername/code-scan-sec/issues
```

---

## مثال كامل لسيناريو استخدام

```bash
# 1. التحقق من الإصدار
security-scan version

# 2. التحقق من التحديثات
security-scan check-update

# 3. فحص المشروع
security-scan scan-local ./myproject --format all

# 4. فحص موقع ويب
security-scan scan-url https://mywebsite.com

# 5. قياس الأداء
security-scan benchmark ./myproject --name "v1.0-benchmark"
```

---

## ملاحظات مهمة

1. **تأكد من استخدام `security-scan` قبل أي أمر فرعي**
2. **الأمر `interactive` يساوي `menu`** - يمكنك استخدام أي منهما
3. **التقارير تُحفظ في مجلد `output` افتراضياً**
4. **يمكنك استخدام `.` للإشارة إلى المجلد الحالي**

---

## الترخيص

MIT License - مفتوح المصدر ومجاني للاستخدام

---

## إصدارات التوثيق

- العربية: [USAGE_GUIDE_AR.md](./USAGE_GUIDE_AR.md)
- English: [README.md](./README.md)

---

**تم التحديث:** 2025-11-19
**الإصدار:** 4.0.0

---

## تواصل معنا

للأسئلة والاقتراحات، يمكنك:
- فتح Issue على GitHub
- التواصل عبر البريد الإلكتروني
- زيارة صفحة التوثيق

---

✨ **ابقَ آمناً، احمِ كودك!** 🔒
